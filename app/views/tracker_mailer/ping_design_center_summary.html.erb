<%= 'The design does not exist at specified location for the following designs'  if @summary[:link_bad].size > 0 %>
<% @summary[:link_bad].each do |design| %>
   <%= sprintf("%s - %s/%s",
           design.directory_name,
           design.design_center.pcb_path,
           design.directory_name) %>
<% end %>

<%= 'The design exists where specified for the following designs'  if @summary[:link_good].size > 0 %>
<% @summary[:link_good].each do |design| %>
   <%= sprintf("%s - %s/%s",
           design.directory_name,
           design.design_center.pcb_path,
           design.directory_name) %>
<% end %>
