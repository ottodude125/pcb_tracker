<%= form_for(@model_document) do |f| %>
  <% if @model_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@model_document.errors.count, "error") %> prohibited this model_document from being saved:</h2>

      <ul>
      <% @model_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
